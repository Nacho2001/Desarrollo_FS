let email = "nm@g.com"

let validar = (mail) => {
for (let index = 0; index < mail.length; index++) {
    const element = mail[index];
    if(element == "@"){
        // desde la posicion del arroba hasta el final, es el dominio del mail
        let dominio = mail.substring()
    }
}
}